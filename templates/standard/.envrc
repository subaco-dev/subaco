# standard テンプレート — direnv フロー。
# リロード契機を明示する（flake / lockfile 変更で devShell を再評価）。
watch_file flake.nix flake.lock uv.lock
use flake

# 注: メモリプレーン（.hive / HIVE_*）・実行プレーン（.cube / E2B_*）の初期化とエクスポートは
#     multi-agent テンプレートでのみ行う（standard は文脈プレーン + 知識プレーンまで）。
